[project]
name = "ominai-hq"
version = "0.0.0"
description = "Oficina digital agentica para transformar una mision de negocio en un Venture Build Package auditable."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = [
    "jsonschema>=4.18,<5",
]
demo = [
    "jsonschema>=4.18,<5",
]
agents = [
    "google-adk==2.8.0",
]
